apiVersion: v1
kind: Service
metadata:
  name: brynjar
  namespace: apps
spec:
  selector:
    app.kubernetes.io/name: proxy
  ports:
  - name: name-of-service-port
    protocol: TCP
    port: 80
    targetPort: 8080
    type: ClusterIP
    ipFamilyPolicy: SingleStack