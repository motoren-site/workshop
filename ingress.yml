apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress-brynjar
  namespace: apps
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx-example
  rules:
  - http:
      paths:
      - path: /brynjar
        pathType: Prefix
        backend:
          service:
            name: test
            port:
              number: 80